const initialGalleryState = {
  loading: false,
  success: false,
  error: null,
};

export const galleryReducer = (state = initialGalleryState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
